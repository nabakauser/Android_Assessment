<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.Android_Assessment" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
    </style>

    <style name="Theme.Android_Assessment" parent="Base.Theme.Android_Assessment">
        <item name="android:windowBackground">@color/dark_green</item>
        <item name="android:statusBarColor">@color/white</item>
    </style>
</resources>